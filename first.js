function add(x,y){
    return parseInt(x) + parseInt(y);
}

function subst(x,y){
    return parseInt(x) - parseInt(y);
}

function multi(x,y){
    return parseInt(x) * parseInt(y);
}

function div(x,y){
    return parseInt(x) / parseInt(y);
}